import React from "react";
import styles from "./ProfileContent.module.css";

const ProfileContent = props => {
	return <div className={styles.profile_content}>ProfileContent</div>;
};

export default ProfileContent;
