import React from "react";
import styles from "./ProfileInfo.module.css";

const ProfileInfo = props => {
	return <div className={styles.profile_info}>ProfileInfo</div>;
};

export default ProfileInfo;
